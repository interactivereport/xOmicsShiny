In this directory we list files to help you set up the R environment to run the xOmicsShiny App.
*renv.lock file to install the packages using renv package. We used R 4.2.0 when setting up the renv environment.
*xOmnicsShiny.yml file for setting up a conda environment
